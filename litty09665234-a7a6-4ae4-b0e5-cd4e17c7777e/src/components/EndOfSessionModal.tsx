import React from 'react';

/**
 * EndOfSessionModal — disabled since session/token gating was removed.
 * This component remains as a no-op placeholder.
 */
const EndOfSessionModal: React.FC<{ onOpenCheckout?: () => void }> = () => null;

export default EndOfSessionModal;
