/**
 * PlaylistShareCard — removed. Playlists are now a single per-user list.
 * This stub is kept to avoid broken imports.
 */
import React from 'react';

const PlaylistShareCard: React.FC = () => null;
export default PlaylistShareCard;
