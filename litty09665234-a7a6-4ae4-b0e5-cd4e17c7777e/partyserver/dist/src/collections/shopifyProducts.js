// Generated by TaroBase SDK Generator: 2026-06-03T13:41:09.710Z
// Collection: shopifyProducts/$productId
// Edits may be overwritten by subsequent runs!
import { get, set, subscribe, count, aggregate, transformValues, formatError, } from '../db-client';
/**
 * Build a ShopifyProducts operation for use with setMany.
 * @returns A DocumentOperation that can be passed to setMany.
 */
export function buildShopifyProducts(productId, data) {
    const doc = transformValues(data);
    return { path: `shopifyProducts/${productId}`, document: doc };
}
/**
 * Any authenticated wallet can create a mapping document. The document ID is the full Shopify product identifier or another app-chosen mapping key, and createdAt must be provided as a Unix timestamp in seconds. (Create/Update Single Item)
 * @returns A boolean indicating whether the operation succeeded (true) or failed (false). Always check this value to confirm the write worked.
 */
export async function setShopifyProducts(productId, data) {
    try {
        const transformedData = transformValues(data);
        const succeeded = await set(`shopifyProducts/${productId}`, transformedData);
        return !!succeeded;
    }
    catch (error) {
        console.error(`Error setting ShopifyProducts: ${formatError(error)}`);
        return false;
    }
}
/**
 *
  Read Operation Details: Anyone can read product-to-song mappings so the frontend and backend can resolve Shopify products to Lit Studios songs.
   (Get Single Item)
 */
export async function getShopifyProducts(productId) {
    try {
        const result = await get(`shopifyProducts/${productId}`);
        return result;
    }
    catch (error) {
        console.error(`Error getting ShopifyProducts: ${formatError(error)}`);
        return null;
    }
}
/**
 * Subscribes to changes in a single ShopifyProducts document. (
  Read Operation Details: Anyone can read product-to-song mappings so the frontend and backend can resolve Shopify products to Lit Studios songs.
  )
 */
export function subscribeShopifyProducts(callback, productId) {
    return subscribe(`shopifyProducts/${productId}`, {
        onData: (data) => {
            callback(data);
        },
        onError: (error) => {
            console.error(`Error subscribing to ShopifyProducts at path '${`shopifyProducts/${productId}`}': ${formatError(error)}`);
        }
    });
}
/**
 * Get many ShopifyProducts items from collection shopifyProducts
 
  Read Operation Details: Anyone can read product-to-song mappings so the frontend and backend can resolve Shopify products to Lit Studios songs.
  
 */
export async function getManyShopifyProducts(filter = "") {
    try {
        const results = await get("shopifyProducts", { prompt: filter });
        return Array.isArray(results) ? results : [];
    }
    catch (error) {
        console.error(`Error getting ShopifyProducts collection at path 'shopifyProducts': ${formatError(error)}`);
        return [];
    }
}
/**
 * Subscribe to changes in ShopifyProducts collection at shopifyProducts
 
  Read Operation Details: Anyone can read product-to-song mappings so the frontend and backend can resolve Shopify products to Lit Studios songs.
  
 */
export function subscribeManyShopifyProducts(callback, filter = "") {
    return subscribe("shopifyProducts", {
        prompt: filter,
        onData: (data) => {
            callback(Array.isArray(data) ? data : []);
        },
        onError: (error) => {
            console.error(`Error subscribing to ShopifyProducts collection at path '${"shopifyProducts"}': ${formatError(error)}`);
        }
    });
}
/**
 * Get all ShopifyProducts items from collection shopifyProducts
 
  Read Operation Details: Anyone can read product-to-song mappings so the frontend and backend can resolve Shopify products to Lit Studios songs.
  
 */
export async function getAllShopifyProducts(filter = "") {
    try {
        const results = await get("shopifyProducts", { prompt: filter });
        return Array.isArray(results) ? results : [];
    }
    catch (error) {
        console.error(`Error getting ShopifyProducts collection at path 'shopifyProducts': ${formatError(error)}`);
        return [];
    }
}
/**
 * Subscribe to changes in ShopifyProducts collection at shopifyProducts
 
  Read Operation Details: Anyone can read product-to-song mappings so the frontend and backend can resolve Shopify products to Lit Studios songs.
  
 */
export function subscribeAllShopifyProducts(callback, filter = "") {
    return subscribe("shopifyProducts", {
        prompt: filter,
        onData: (data) => {
            callback(Array.isArray(data) ? data : []);
        },
        onError: (error) => {
            console.error(`Error subscribing to ShopifyProducts collection at path '${"shopifyProducts"}': ${formatError(error)}`);
        }
    });
}
/**
 * Count ShopifyProducts items in collection shopifyProducts.
 * Returns a numeric count instead of fetching full documents — much more efficient.
 *
 * NOTE: This only works because the read policy for this collection is "true".
 * @param filter - Optional natural language filter (e.g., "created in the last 7 days")
 * @returns AggregateResult with the count value
 */
export async function countShopifyProducts(filter = "") {
    try {
        return await count("shopifyProducts", { prompt: filter || undefined });
    }
    catch (error) {
        console.error(`Error counting ShopifyProducts at path 'shopifyProducts': ${formatError(error)}`);
        return { value: 0 };
    }
}
/**
 * Run an aggregate operation on ShopifyProducts items in collection shopifyProducts.
 * Returns a single numeric value instead of fetching full documents.
 *
 * Supported operations: 'count', 'uniqueCount', 'sum', 'avg', 'min', 'max'.
 * For 'sum', 'avg', 'min', 'max', and 'uniqueCount', you must provide the field name.
 *
 * NOTE: This only works because the read policy for this collection is "true".
 * @param operation - The aggregate operation to perform
 * @param opts - Options including optional filter prompt and field name
 * @returns AggregateResult with the computed numeric value
 */
export async function aggregateShopifyProducts(operation, opts) {
    try {
        return await aggregate("shopifyProducts", operation, { prompt: opts?.filter || undefined, field: opts?.field });
    }
    catch (error) {
        console.error(`Error aggregating ShopifyProducts at path 'shopifyProducts': ${formatError(error)}`);
        return { value: 0 };
    }
}
