// Generated by TaroBase SDK Generator: 2026-06-03T13:41:09.711Z
// Collection: gmStreaks/$userAddress
// Edits may be overwritten by subsequent runs!
import { get, set, subscribe, count, aggregate, transformValues, formatError, } from '../db-client';
/**
 * Build a GmStreaks operation for use with setMany.
 * @returns A DocumentOperation that can be passed to setMany.
 */
export function buildGmStreaks(userAddress, data) {
    const doc = data ? transformValues(data) : {};
    return { path: `gmStreaks/${userAddress}`, document: doc };
}
/**
 * Users create their own streak record on first GM. The $userAddress path param must equal caller's wallet. currentStreak and totalGms should start at 1, longestStreak at 1, and lastGmDate set to today's date string. (Create/Update Single Item)
 * @returns A boolean indicating whether the operation succeeded (true) or failed (false). Always check this value to confirm the write worked.
 */
export async function setGmStreaks(userAddress, data) {
    try {
        const transformedData = transformValues(data || {});
        const succeeded = await set(`gmStreaks/${userAddress}`, transformedData);
        return !!succeeded;
    }
    catch (error) {
        console.error(`Error setting GmStreaks: ${formatError(error)}`);
        return false;
    }
}
/**
 * Build a GmStreaks update operation for use with setMany.
 * @returns A DocumentOperation that can be passed to setMany.
 */
export function buildUpdateGmStreaks(userAddress, data) {
    const doc = transformValues(data);
    return { path: `gmStreaks/${userAddress}`, document: doc };
}
/**
 * Users update their own streak record when sending subsequent GMs. Frontend logic handles streak continuation vs reset based on lastGmDate comparison to current date. (Update Single Item)
 * @returns A boolean indicating whether the operation succeeded (true) or failed (false). Always check this value to confirm the update worked.
 */
export async function updateGmStreaks(userAddress, data) {
    try {
        const transformedData = transformValues(data);
        const succeeded = await set(`gmStreaks/${userAddress}`, transformedData);
        return !!succeeded;
    }
    catch (error) {
        console.error(`Error updating GmStreaks: ${formatError(error)}`);
        return false;
    }
}
/**
 *
  Read Operation Details: Public read. Anyone can view GM streak data for any user.
   (Get Single Item)
 */
export async function getGmStreaks(userAddress) {
    try {
        const result = await get(`gmStreaks/${userAddress}`);
        return result;
    }
    catch (error) {
        console.error(`Error getting GmStreaks: ${formatError(error)}`);
        return null;
    }
}
/**
 * Subscribes to changes in a single GmStreaks document. (
  Read Operation Details: Public read. Anyone can view GM streak data for any user.
  )
 */
export function subscribeGmStreaks(callback, userAddress) {
    return subscribe(`gmStreaks/${userAddress}`, {
        onData: (data) => {
            callback(data);
        },
        onError: (error) => {
            console.error(`Error subscribing to GmStreaks at path '${`gmStreaks/${userAddress}`}': ${formatError(error)}`);
        }
    });
}
/**
 * Get many GmStreaks items from collection gmStreaks
 
  Read Operation Details: Public read. Anyone can view GM streak data for any user.
  
 */
export async function getManyGmStreaks(filter = "") {
    try {
        const results = await get("gmStreaks", { prompt: filter });
        return Array.isArray(results) ? results : [];
    }
    catch (error) {
        console.error(`Error getting GmStreaks collection at path 'gmStreaks': ${formatError(error)}`);
        return [];
    }
}
/**
 * Subscribe to changes in GmStreaks collection at gmStreaks
 
  Read Operation Details: Public read. Anyone can view GM streak data for any user.
  
 */
export function subscribeManyGmStreaks(callback, filter = "") {
    return subscribe("gmStreaks", {
        prompt: filter,
        onData: (data) => {
            callback(Array.isArray(data) ? data : []);
        },
        onError: (error) => {
            console.error(`Error subscribing to GmStreaks collection at path '${"gmStreaks"}': ${formatError(error)}`);
        }
    });
}
/**
 * Get all GmStreaks items from collection gmStreaks
 
  Read Operation Details: Public read. Anyone can view GM streak data for any user.
  
 */
export async function getAllGmStreaks(filter = "") {
    try {
        const results = await get("gmStreaks", { prompt: filter });
        return Array.isArray(results) ? results : [];
    }
    catch (error) {
        console.error(`Error getting GmStreaks collection at path 'gmStreaks': ${formatError(error)}`);
        return [];
    }
}
/**
 * Subscribe to changes in GmStreaks collection at gmStreaks
 
  Read Operation Details: Public read. Anyone can view GM streak data for any user.
  
 */
export function subscribeAllGmStreaks(callback, filter = "") {
    return subscribe("gmStreaks", {
        prompt: filter,
        onData: (data) => {
            callback(Array.isArray(data) ? data : []);
        },
        onError: (error) => {
            console.error(`Error subscribing to GmStreaks collection at path '${"gmStreaks"}': ${formatError(error)}`);
        }
    });
}
/**
 * Count GmStreaks items in collection gmStreaks.
 * Returns a numeric count instead of fetching full documents — much more efficient.
 *
 * NOTE: This only works because the read policy for this collection is "true".
 * @param filter - Optional natural language filter (e.g., "created in the last 7 days")
 * @returns AggregateResult with the count value
 */
export async function countGmStreaks(filter = "") {
    try {
        return await count("gmStreaks", { prompt: filter || undefined });
    }
    catch (error) {
        console.error(`Error counting GmStreaks at path 'gmStreaks': ${formatError(error)}`);
        return { value: 0 };
    }
}
/**
 * Run an aggregate operation on GmStreaks items in collection gmStreaks.
 * Returns a single numeric value instead of fetching full documents.
 *
 * Supported operations: 'count', 'uniqueCount', 'sum', 'avg', 'min', 'max'.
 * For 'sum', 'avg', 'min', 'max', and 'uniqueCount', you must provide the field name.
 *
 * NOTE: This only works because the read policy for this collection is "true".
 * @param operation - The aggregate operation to perform
 * @param opts - Options including optional filter prompt and field name
 * @returns AggregateResult with the computed numeric value
 */
export async function aggregateGmStreaks(operation, opts) {
    try {
        return await aggregate("gmStreaks", operation, { prompt: opts?.filter || undefined, field: opts?.field });
    }
    catch (error) {
        console.error(`Error aggregating GmStreaks at path 'gmStreaks': ${formatError(error)}`);
        return { value: 0 };
    }
}
