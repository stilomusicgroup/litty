// Generated by TaroBase SDK Generator: 2026-06-03T13:41:09.714Z
// Collection: userRealizedPnL/$userAddress/events/$eventId
// Edits may be overwritten by subsequent runs!
import { get, set, subscribe, count, aggregate, transformValues, formatError, } from '../db-client';
/**
 * Build a UserRealizedPnLEvents operation for use with setMany.
 * @returns A DocumentOperation that can be passed to setMany.
 */
export function buildUserRealizedPnLEvents(userAddress, eventId, data) {
    const doc = data ? transformValues(data) : {};
    return { path: `userRealizedPnL/${userAddress}/events/${eventId}`, document: doc };
}
/**
 * Authenticated users only. Caller must equal $userAddress. Frontend creates a PnL event after each successful sell, calculating gain/loss from consumed lots. (Create/Update Single Item)
 * @returns A boolean indicating whether the operation succeeded (true) or failed (false). Always check this value to confirm the write worked.
 */
export async function setUserRealizedPnLEvents(userAddress, eventId, data) {
    try {
        const transformedData = transformValues(data || {});
        const succeeded = await set(`userRealizedPnL/${userAddress}/events/${eventId}`, transformedData);
        return !!succeeded;
    }
    catch (error) {
        console.error(`Error setting UserRealizedPnLEvents: ${formatError(error)}`);
        return false;
    }
}
/**
 *
  Read Operation Details: Anyone can read realized PnL events. Public for portfolio transparency.
   (Get Single Item)
 */
export async function getUserRealizedPnLEvents(userAddress, eventId) {
    try {
        const result = await get(`userRealizedPnL/${userAddress}/events/${eventId}`);
        return result;
    }
    catch (error) {
        console.error(`Error getting UserRealizedPnLEvents: ${formatError(error)}`);
        return null;
    }
}
/**
 * Subscribes to changes in a single UserRealizedPnLEvents document. (
  Read Operation Details: Anyone can read realized PnL events. Public for portfolio transparency.
  )
 */
export function subscribeUserRealizedPnLEvents(callback, userAddress, eventId) {
    return subscribe(`userRealizedPnL/${userAddress}/events/${eventId}`, {
        onData: (data) => {
            callback(data);
        },
        onError: (error) => {
            console.error(`Error subscribing to UserRealizedPnLEvents at path '${`userRealizedPnL/${userAddress}/events/${eventId}`}': ${formatError(error)}`);
        }
    });
}
/**
 * Get many UserRealizedPnLEvents items from collection userRealizedPnL/${userAddress}/events
 
  Read Operation Details: Anyone can read realized PnL events. Public for portfolio transparency.
  
 */
export async function getManyUserRealizedPnLEvents(userAddress, filter = "") {
    try {
        const results = await get(`userRealizedPnL/${userAddress}/events`, { prompt: filter });
        return Array.isArray(results) ? results : [];
    }
    catch (error) {
        console.error(`Error getting UserRealizedPnLEvents collection at path 'userRealizedPnL/${userAddress}/events': ${formatError(error)}`);
        return [];
    }
}
/**
 * Subscribe to changes in UserRealizedPnLEvents collection at userRealizedPnL/${userAddress}/events
 
  Read Operation Details: Anyone can read realized PnL events. Public for portfolio transparency.
  
 */
export function subscribeManyUserRealizedPnLEvents(callback, userAddress, filter = "") {
    return subscribe(`userRealizedPnL/${userAddress}/events`, {
        prompt: filter,
        onData: (data) => {
            callback(Array.isArray(data) ? data : []);
        },
        onError: (error) => {
            console.error(`Error subscribing to UserRealizedPnLEvents collection at path '${`userRealizedPnL/${userAddress}/events`}': ${formatError(error)}`);
        }
    });
}
/**
 * Get all UserRealizedPnLEvents items from collection userRealizedPnL/${userAddress}/events
 
  Read Operation Details: Anyone can read realized PnL events. Public for portfolio transparency.
  
 */
export async function getAllUserRealizedPnLEvents(userAddress, filter = "") {
    try {
        const results = await get(`userRealizedPnL/*/events`, { prompt: filter });
        return Array.isArray(results) ? results : [];
    }
    catch (error) {
        console.error(`Error getting UserRealizedPnLEvents collection at path 'userRealizedPnL/${userAddress}/events': ${formatError(error)}`);
        return [];
    }
}
/**
 * Subscribe to changes in UserRealizedPnLEvents collection at userRealizedPnL/${userAddress}/events
 
  Read Operation Details: Anyone can read realized PnL events. Public for portfolio transparency.
  
 */
export function subscribeAllUserRealizedPnLEvents(callback, userAddress, filter = "") {
    return subscribe(`userRealizedPnL/*/events`, {
        prompt: filter,
        onData: (data) => {
            callback(Array.isArray(data) ? data : []);
        },
        onError: (error) => {
            console.error(`Error subscribing to UserRealizedPnLEvents collection at path '${`userRealizedPnL/*/events`}': ${formatError(error)}`);
        }
    });
}
/**
 * Count UserRealizedPnLEvents items in collection userRealizedPnL/${userAddress}/events.
 * Returns a numeric count instead of fetching full documents — much more efficient.
 *
 * NOTE: This only works because the read policy for this collection is "true".
 * @param filter - Optional natural language filter (e.g., "created in the last 7 days")
 * @returns AggregateResult with the count value
 */
export async function countUserRealizedPnLEvents(userAddress, filter = "") {
    try {
        return await count(`userRealizedPnL/${userAddress}/events`, { prompt: filter || undefined });
    }
    catch (error) {
        console.error(`Error counting UserRealizedPnLEvents at path 'userRealizedPnL/${userAddress}/events': ${formatError(error)}`);
        return { value: 0 };
    }
}
/**
 * Run an aggregate operation on UserRealizedPnLEvents items in collection userRealizedPnL/${userAddress}/events.
 * Returns a single numeric value instead of fetching full documents.
 *
 * Supported operations: 'count', 'uniqueCount', 'sum', 'avg', 'min', 'max'.
 * For 'sum', 'avg', 'min', 'max', and 'uniqueCount', you must provide the field name.
 *
 * NOTE: This only works because the read policy for this collection is "true".
 * @param operation - The aggregate operation to perform
 * @param opts - Options including optional filter prompt and field name
 * @returns AggregateResult with the computed numeric value
 */
export async function aggregateUserRealizedPnLEvents(userAddress, operation, opts) {
    try {
        return await aggregate(`userRealizedPnL/${userAddress}/events`, operation, { prompt: opts?.filter || undefined, field: opts?.field });
    }
    catch (error) {
        console.error(`Error aggregating UserRealizedPnLEvents at path 'userRealizedPnL/${userAddress}/events': ${formatError(error)}`);
        return { value: 0 };
    }
}
